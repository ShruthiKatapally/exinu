#include <prodcons.h>

uint future_cons(future *fut) {
  int i, status;
  status = future_get(fut, &i);
  if (status == SYSERR) {
    kprintf("future_get failed\n");
    return -1;
  }
  kprintf("consumed %d\n", i);
  return OK;
}

